# H2 contact sheet

`velde.png` and `kracht.png` are the two brand columns; `contact-sheet.png` puts them side by side.
All three are written by `bun bench brand-divergence`, so none of them can drift from the build.

Reviewed as a SET, side by side — a per-block checklist cannot see repetition or incoherence
[TASKS.md §2]. The H2 floor in `bench/checks/divergence.ts` was pinned against this sheet.
