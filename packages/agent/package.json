{
  "name": "@maximal/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/boot.ts --outfile dist/agent.js --target=browser --format=iife --minify"
  },
  "dependencies": {
    "@maximal/tokens": "workspace:*"
  }
}
